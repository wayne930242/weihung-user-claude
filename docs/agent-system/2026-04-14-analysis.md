# Agent System Analysis Report

**Date:** 2026-04-14 00:00
**Project:** weihung-user-claude (personal user-root agent system for Claude Code + Codex)

## Project Overview

| Aspect | Finding |
|--------|---------|
| Language(s) | Bash (installer scripts), Markdown (prompts, rules, skills) |
| Framework(s) | None — personal dotfiles / agent config repo |
| Project type | Personal user-root agent system — global Claude Code + Codex config managed via symlink installer |
| CI/CD | None (solo personal repo) |
| Testing | Bash test scripts (`tests/`) |
| Linting | None detected |
| Team scale | Solo (1 committer: wayne930242@gmail.com, 3 remote branches) |
| Notable | `docs/design-principles.md` mandates "keep user-root layer thin". Installer uses symlinks, backup-first, add-only merge. rcc plugin marketplace installed separately at `~/.claude/plugins/`. |

---

## Component Inventory

| # | Type | Scope | Path | Lines | Status |
|---|------|-------|------|-------|--------|
| 1 | CLAUDE.md | user-root | `~/.claude/CLAUDE.md` → `projects/weihung-user-claude/CLAUDE.md` | 24 | OK |
| 2 | shared | user-root | `projects/weihung-user-claude/shared/communication.md` | 17 | OK |
| 3 | shared | user-root | `projects/weihung-user-claude/shared/engineering.md` | 64 | OK |
| 4 | shared | user-root | `projects/weihung-user-claude/shared/context-management.md` | 6 | OK |
| 5 | rule (global) | user-root | `~/.claude/rules/chinese-writing.md` | 9 | OK |
| 6 | rule (global) | user-root | `~/.claude/rules/clean-architecture.md` | 21 | OK |
| 7 | rule (global) | user-root | `~/.claude/rules/dependencies.md` | 7 | OK |
| 8 | rule (global) | user-root | `~/.claude/rules/deployment.md` | 9 | OK |
| 9 | rule (path-scoped) | user-root | `~/.claude/rules/go.md` → `**/*.go` | 11 | OK |
| 10 | rule (path-scoped) | user-root | `~/.claude/rules/markdown.md` → `**/*.md` | 12 | OK |
| 11 | rule (path-scoped) | user-root | `~/.claude/rules/python.md` → `**/*.py` | 11 | OK |
| 12 | rule (path-scoped) | user-root | `~/.claude/rules/shell.md` → `**/*.sh` | 11 | OK |
| 13 | rule (path-scoped) | user-root | `~/.claude/rules/typescript.md` → `**/*.{ts,tsx}` | 11 | OK |
| 14 | hook | user-root | `~/.claude/hooks/log-notification.sh` | 30 | OK |
| 15 | hook | user-root | `~/.claude/hooks/log-stop.sh` | 30 | OK |
| 16 | hook | user-root | `~/.claude/hooks/rtk-rewrite.sh` (untracked, RTK-managed) | 98 | NEEDS_FIX |
| 17 | agent | user-root | `~/.claude/agents/security-reviewer.md` | 5 | NEEDS_FIX |
| 18 | agent | user-root | `~/.claude/agents/silent-failure-hunter.md` | 5 | NEEDS_FIX |
| 19 | skill | user-root | `~/.claude/skills/inspecting/` | 72 | OK |
| 20 | skill | user-root | `~/.claude/skills/investigating/` | 44 | NEEDS_FIX |
| 21 | skill | user-root | `~/.claude/skills/leveraging-tasks/` | 140 | NEEDS_FIX |
| 22 | skill | user-root | `~/.claude/skills/providing-knowledge/` | 31 | OK |
| 23 | skill | user-root | `~/.claude/skills/reflecting-to-root/` | 85 | NEEDS_FIX |
| 24 | skill (plugin) | user-root | `~/.claude/skills/agent-browser/` | ~200 | OK |
| 25 | skill (symlink) | user-root | `~/.claude/skills/find-skills` → `.agents/skills/` | — | OK |
| 26 | skill (plugin) | user-root | `~/.claude/skills/openspec-*/` (4 skills) | varies | OK |

**Gap analysis (user-root vs project-level):**
- This IS the user-root repo — no project-level specialization expected per design.
- `rtk-rewrite.sh` hook exists in `~/.claude/hooks/` but is NOT tracked in source repo — managed by RTK tool itself. Expected, but undocumented.
- `agent-browser`, `openspec-*` installed in `~/.claude/skills/` but not in `projects/weihung-user-claude/skills/` — these come from rcc plugin and other sources. Source repo only tracks 5 core skills.

---

## Weakness Findings

> Includes pipeline chain-break, state persistence, and mode mismatch checks

### CRITICAL (must fix)

None found.

### WARNING (should fix)

| # | Category | Component | Finding | Suggested Fix |
|---|----------|-----------|---------|---------------|
| W1 | 2 — Context | `agents/security-reviewer.md`, `agents/silent-failure-hunter.md` | Reviewer/analyzer subagents lack `context: fork` frontmatter — they inherit full parent conversation context, risking context pollution during review | Add `context: fork` to both agent frontmatter blocks |
| W2 | 1 — Routing | `skills/investigating/` | Description includes "check the state of something" — overlaps with `inspecting` trigger ("check, audit, verify, review"), creating ambiguous routing when user says "check X" | Remove "check the state of something" from investigating description; narrow to "research, find out, investigate a specific question" |
| W3 | 4 — Redundancy | `rules/deployment.md` + `skills/leveraging-tasks/` | Deploy safety checklist (rsync --delete, Ansible --check, test suite, target directory) duplicated verbatim in both `deployment.md` rule and `leveraging-tasks` Deploy pipe entry gate — divergence risk if one is updated | Remove deploy checklist from `leveraging-tasks` Deploy section; replace with "Follow `~/.claude/rules/deployment.md`" |
| W4 | 1 — Routing | `skills/leveraging-tasks/`, `skills/reflecting-to-root/` | Both skills predate rcc conventions — missing `## Routing` section declaring Pattern, Handoff, and Next | Add structured `## Routing` section to each; include flowchart for `reflecting-to-root` |

### INFO (nice to fix)

| # | Category | Component | Finding | Suggested Fix |
|---|----------|-----------|---------|---------------|
| I1 | 5 — Security | `~/.claude/hooks/rtk-rewrite.sh` | PreToolUse Bash hook has no explicit timeout in `settings.json` — default timeout may be too long for an inline rewrite hook | Add `"timeout": 10` to the rtk hook entry in settings.json |
| I2 | 9 — Project Context | user-root | No PostToolUse hooks for language validation (go build, tsc --noEmit) — catch errors earlier | Add PostToolUse hooks for Go/TS if the cost is acceptable; intentional per design-principles.md ("minimal hooks") |
| I3 | 6 — Observability | `skills/leveraging-tasks/` | Routing decisions (implement/design/debug/deploy) produce no log — can't audit what path was taken | Add a one-line routing decision log at start of each pipe branch |
| I4 | 11 — Rules Health | global rules | 4 global rules (46 lines total) load on every session regardless of task type — minor overhead | Keep monitoring; consider `paths:` scoping if any rule grows larger or becomes task-specific |

### Pipeline Checks

| Check | Result |
|-------|--------|
| Chain break: `inspecting` → `investigating` | OK — `investigating` exists and accepts check-item input |
| Orphan skills | None — all 5 source skills have inbound routes from CLAUDE.md or other skills |
| Dead ends | None — all skills have explicit termination or handoff |
| State persistence (`inspecting` chain) | OK for short chains — no state file needed at current scope |
| Pipeline mode | `inspecting` = chain-pipe (2 distinct skills); `leveraging-tasks` = owner-pipe (2-3 steps, single dispatcher). Both appropriately sized. |

---

## Restructuring Recommendations

### Merge Recommendations

| # | ID | Components | Rationale | Priority |
|---|----|------------|-----------|----------|
| — | — | — | No merge candidates identified | — |

### Extract / Reconcile Recommendations

#### EXTRACT-1: Deduplicate deploy safety in `leveraging-tasks`

**Priority:** MEDIUM
**Type:** Extract (reconcile)
**Affected files:**
- `projects/weihung-user-claude/skills/leveraging-tasks/SKILL.md`
- `~/.claude/rules/deployment.md`

**Migration table:**

| Before | After | Migration Steps |
|--------|-------|-----------------|
| `leveraging-tasks` Deploy entry gate contains 4 safety rules verbatim | `leveraging-tasks` Deploy entry gate references the rule only | 1. Remove lines 81–85 from `leveraging-tasks` SKILL.md Deploy entry gate. 2. Replace with: "Apply all requirements in `~/.claude/rules/deployment.md`." 3. Verify no content lost. |

**Reason:** Weakness W3 — same deploy safety checklist maintained in two places; divergence risk.

---

### New Component Recommendations

#### NEW-1: Add `context: fork` to subagents

**Priority:** MEDIUM
**Type:** New (frontmatter fix)
**Affected files:**
- `projects/weihung-user-claude/claude/agents/security-reviewer.md`
- `projects/weihung-user-claude/claude/agents/silent-failure-hunter.md`

**Action:** Add `context: fork` to frontmatter of both agent definitions (if supported by current Claude Code version).

**Reason:** Weakness W1 — reviewer subagents inherit full parent context; fork isolation prevents context pollution.

---

#### NEW-2: Narrow `investigating` trigger description

**Priority:** MEDIUM
**Type:** New (description fix)
**Affected files:**
- `projects/weihung-user-claude/skills/investigating/SKILL.md`

**Action:** Change "check the state of something" in the description to "investigate a specific question or claim" to eliminate overlap with `inspecting` triggers.

**Reason:** Weakness W2 — overlapping trigger creates ambiguous routing when user says "check X".

---

#### NEW-3: Add Routing section + flowchart to legacy skills

**Priority:** LOW
**Type:** New (structural alignment)
**Affected files:**
- `projects/weihung-user-claude/skills/leveraging-tasks/SKILL.md`
- `projects/weihung-user-claude/skills/reflecting-to-root/SKILL.md`

**Action:** Add `## Routing` section to each skill with Pattern/Handoff/Next. Add dot flowchart to `reflecting-to-root`.

**Reason:** Weakness W4 — skills predate rcc conventions; missing structured routing declarations.

---

#### NEW-4: Add timeout to rtk-rewrite hook in settings.json

**Priority:** LOW
**Type:** New (config fix)
**Affected files:**
- `~/.claude/settings.json` (PreToolUse Bash hook entry)

**Action:** Add `"timeout": 10` to the rtk-rewrite.sh hook object in settings.json.

**Reason:** Weakness I1 — no explicit timeout for inline PreToolUse hook.

---

### Restructuring Summary

| Type | Count | HIGH | MEDIUM | LOW |
|------|-------|------|--------|-----|
| Merge | 0 | 0 | 0 | 0 |
| Extract | 1 | 0 | 1 | 0 |
| New | 4 | 0 | 2 | 2 |
| Remove | 0 | 0 | 0 | 0 |
| **Total** | **5** | **0** | **3** | **2** |

---

## Rules Health Summary

| Metric | Value | Status |
|--------|-------|--------|
| CLAUDE.md lines | 24 | ok |
| Global rules count / lines | 4 rules / 46 lines | ok |
| Session-start total lines | ~177 (CLAUDE.md + @shared/* + RTK.md + global rules) | ok |
| Path-scoped rules | 5 (go, python, shell, typescript, markdown) | ok |
| Rules with procedural content | 0 | ok |
| Dead glob patterns | 0 (user-root globs are alive across 67 projects) | ok |

---

## Summary

- Components scanned: 26
- Critical issues: 0
- Warnings: 4
- Info: 4
- Merge recommendations: 0
- Extract recommendations: 1
- New recommendations: 4

**Overall assessment:** The user-root agent system is well-structured and deliberately thin, consistent with the stated design principle of keeping the user-root layer minimal. All rules are appropriately scoped, CLAUDE.md is compact, and the skill pipeline (`inspecting` → `investigating`) is clean. The 4 warnings are low-risk maintenance issues rather than operational failures. No critical blockers. Priority fix: add `context: fork` to subagents (W1) and deduplicate deploy safety content (W3).

**Recommended next step:** Proceed to `planning-agent-systems` — recommendations are clear and actionable.
